Issues noted:
1. For AI Profit-Optimized, if you remember, it should also the margin, on how they should be sequenced. Did you forget already? That's why upon seeing the sequences from each option, it feels nothing has changed.
2. Be sure to follow these:
- For standard sequence, its just a rule-based that sorts the orders chronological based from its avail date coming from MTO orders, and N10D target dates
- For AI balanced sequence, it uses AI to analyze the orders, sorts the orders and how they should be placed for maximum optimization, and suggest dates of when these orders can be produced. For example, the AI determined to switch order C with Order A because doing so will result to lower changeover to order B. rather than just stay with order a then b then c, it will look like order A > changeover > order B > changeover > order C. The AI determines that order C can be switch so that order C > low changeover > order b > changeover > order A. Then suggest dates when best they can be produce according to AI analysis
- For AI Profit-Optimized, similar with balanced sequence, but with added rule which is the margins, that the higher the margin is, they can be prioritized.
3. Just make sure that actual dates, critical, urgent, and monitor dates, won't be compromise that will result in a delay. The AI can also explore on the stock sufficient orders, since they are stock sufficient, meaning they don't have like target dates unlike with critical, urgent, or monitor. Since we only have 10 days from the N10D, if order doesn't have red highlights / threshold, it means that these orders can be produced beyond the target date (since they don't have target date). For example, if order A doesn't have a target date because in the next 10 days, it won't go beyond the threshold, the AI can suggest dates beyond the its target date. So for example, if order A's target date is April 30 (day 10), the AI can suggest dates maybe may 8 or may 9. And this will be helpful to lower the changeovers because for example, if we have three orders, order A, B, and C. Order A's target date is April 30 (day 10) and Order B is May 10, and Order C is also May 10. Order B and C have different materials (category, color, diameter), but order A and C are similar. Since order A have no strict target date because its stock sufficient, they the AI can suggest to place order A next to order C, to minimize the changeovers

Don't hesitate to ask