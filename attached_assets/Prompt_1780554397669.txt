Please update the **AI vs Final Sequence — Adjustment Details** table so that the **Reason** column is fully aligned to the current feature behavior, generated fresh by AI when the preview loads, and the table columns are reordered and cleaned up for readability.

---

## 1. REMOVE THE OLD TEMPLATE LANGUAGE FROM THE `REASON` COLUMN

There are still old template-based explanation fragments appearing in the **Reason** column.

### Examples of old wording that should be removed
Please remove legacy/template phrasing such as:
- `Repositioned by date-first sort...`
- `No deterministic rules changed any dates`
- `hard ceiling`
- `monitor future-dispatch`
- and other leftover explanation text from older logic/templates

### Goal
The Reason column should no longer contain mixed old template wording from the past.

### Expected result
The explanation text should feel clean, current, and consistent with the present AI-driven sequence behavior.

---

## 2. GENERATE THE `REASON` COLUMN USING AI WHEN THE PREVIEW LOADS

Please generate the **Reason** text using AI once the preview is loaded.

### Required behavior
After the preview and its final visible sequence are ready, generate the row-level Reason values based on the actual loaded preview result.

### The AI-generated reason should use the real loaded preview context, such as:
- Final Rank
- Date Rank
- Strategy Rank
- Item Description
- Current Line
- Proposed Line
- Moved?
- Repositioned?
- suggested/effective date if relevant
- and the actual final row placement

### Goal
Make the Reason column explain what really happened in the loaded preview, instead of relying on stale template logic.

### Expected result
Reason text becomes more accurate, more natural, and better aligned to the actual preview result.

---

## 3. USE TEMPLATE TEXT ONLY AS A FALLBACK IF AI REASON GENERATION FAILS

If AI reason generation fails, times out, or returns invalid output, then you may fall back to a simpler template-based reason.

### Important
This fallback should be used only when necessary.

### Goal
Keep the app stable while making AI-generated reasoning the primary behavior.

### Expected result
The normal/default behavior is AI-generated reasons; old template logic is fallback only.

---

## 4. REORDER THE TABLE COLUMNS

Please move **Date Rank** and **Strategy Rank** so they sit beside **Final Rank**.

### New required table structure
Please use this exact column order:

- **Final Rank**
- **Date Rank**
- **Strategy Rank**
- **Order ID**
- **Item Description**
- **Current Line**
- **Proposed Line**
- **Moved?**
- **Repositioned?**
- **Reason**

### Goal
Keep the three ranking-related columns together so users can compare them more easily.

### Expected result
The ranking information becomes easier to scan and understand.

---

## 5. MAKE THE COLUMN HEADERS DISPLAY IN ONE LINE ONLY

Please make the table headers display in a single line only.

### Important
Do not allow header labels like:
- `Final Rank`
- `Date Rank`
- `Strategy Rank`
- `Current Line`
- `Proposed Line`

to wrap into two lines.

### Goal
Improve readability and make the table look cleaner.

### Expected result
Each column header appears on a single line.

---

## 6. KEEP THE `REASON` SIMPLE, CLEAR, AND ALIGNED TO WHAT ACTUALLY HAPPENED

Please make the AI-generated Reason text simple and understandable.

### The reason should explain things like:
- this order stayed in place because date rank and strategy rank aligned
- this order moved earlier because its date rank placed it ahead
- this order was repositioned because another order had an earlier suggested date
- this order stayed within the same date group and strategy rank determined the order
- this order moved lines and was then placed according to its date/strategy position

### Goal
Make the Reason column easy to understand without technical or outdated wording.

### Expected result
The Reason text clearly explains what happened in plain language.

---

## IMPORTANT

Please update the **AI vs Final Sequence — Adjustment Details** table so that:

- old template wording is removed from the **Reason** column
- the **Reason** is generated by AI once the preview loads
- old template logic is used only as a fallback if AI generation fails
- the columns are reordered to:

  - **Final Rank**
  - **Date Rank**
  - **Strategy Rank**
  - **Order ID**
  - **Item Description**
  - **Current Line**
  - **Proposed Line**
  - **Moved?**
  - **Repositioned?**
  - **Reason**

- all headers display in **one line only**

This is needed so the table better reflects the current AI-driven sequencing behavior and is easier for users to read and understand.

---

## DEBUG LOGGING

Temporarily log AI reason generation and the new table structure.

```javascript
console.debug('[AI vs Final Sequence Reason Generation On Preview Load]', {
  previewLoaded: true,
  aiReasonGenerationTriggered: true,
  fallbackTemplateUsed: false,
});
```

```javascript
console.debug('[AI vs Final Sequence Table Column Order]', {
  columns: [
    'Final Rank',
    'Date Rank',
    'Strategy Rank',
    'Order ID',
    'Item Description',
    'Current Line',
    'Proposed Line',
    'Moved?',
    'Repositioned?',
    'Reason',
  ],
});
```

```javascript
console.debug('[AI vs Final Sequence One-Line Headers]', {
  oneLineHeadersApplied: true,
});
```

---

## VERIFICATION

1. ✅ Old template wording is removed from the Reason column
2. ✅ Reason is generated by AI when the preview loads
3. ✅ Template text is used only as fallback
4. ✅ Date Rank and Strategy Rank are moved beside Final Rank
5. ✅ Table uses the new requested column order
6. ✅ Headers display in one line only
7. ✅ Reason text is clearer and aligned to the actual preview behavior
8. ✅ No UI errors or console errors are introduced